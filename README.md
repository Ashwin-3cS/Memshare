# Memshare

Memshare is a repo that contains:

- `relayer/`: the MemWal-derived relayer and Walrus or SEAL sidecar pieces
- `cli/`: the Memshare CLI that will capture, push, recall, and attach project facts

Planning docs currently live at the repo root while the implementation is being bootstrapped.
